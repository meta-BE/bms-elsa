## 新機能
- 難易度表設定モーダルに難易度表のドラッグ&ドロップ並び替え機能を追加。設定した順序は難易度表セレクターにも反映される（`difficulty_table`に`sort_order`カラムを追加）。
- 難易度表の追加・削除・更新を、共通設定モーダルから難易度表画面側の独立モーダルに分離。難易度表画面のヘッダーから直接「難易度表設定」を開けるようにし、モーダルclose時にテーブル一覧とエントリを自動再取得。
- 重複スキャン画面（DuplicateView）に上下キーによるキーボードナビゲーションを追加し、グループをキー操作で順次選択できるようにした。
