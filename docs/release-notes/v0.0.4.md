## 新機能
- BMSファイルからWAV定義を抽出するパーサーを追加（RANDOM/IF 1ブロックに対応）。
- WAV集合の類似度比較を行うMinHash計算ライブラリを追加し、`chart_meta`テーブルに`wav_minhash`カラムを追加。
- 譜面一覧タブから登録済み譜面のWAV MinHashを一括計算する「MinHashスキャン」機能を追加（進捗表示・中断対応）。
- 譜面一覧テーブルにNOTES列を追加し、譜面情報カードにもノート数を表示。
- URL書き換えルールのCSVシードデータをマイグレーション時に冪等投入するように追加。
